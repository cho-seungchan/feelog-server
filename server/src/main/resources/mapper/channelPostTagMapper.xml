<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.feelog.mapper.ChannelPostTagMapper">

    <insert id="insert" parameterType="ChannelPostTagVO">
        INSERT INTO tbl_channel_post_tag (
            id,               -- tag_id
            channel_post_id
        )
        VALUES (
                   #{id},
                   #{channelPostId}
               )
    </insert>

    <delete id="deleteByChannelPostId" parameterType="long">
        DELETE FROM tbl_channel_post_tag
        WHERE channel_post_id = #{channelPostId}
    </delete>

    <select id="findTagContentsByChannelPostId" parameterType="long" resultType="string">
        SELECT t.contents
        FROM tbl_channel_post_tag cpt
                 JOIN tbl_tag t ON cpt.id = t.id
        WHERE cpt.channel_post_id = #{channelPostId}
    </select>

</mapper>